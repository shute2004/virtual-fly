[project]
name = "virtual-fly-tools"
version = "0.1.0"
description = "Data preparation and analysis tools for virtual-fly"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.2",
    "pyarrow>=18.0",
]

[tool.uv]
package = false
